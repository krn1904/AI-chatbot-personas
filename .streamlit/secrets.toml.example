# Local Streamlit secrets template.
# Copy this file to .streamlit/secrets.toml for local `streamlit run`, OR set the
# same key in the Streamlit Community Cloud dashboard (Settings -> Secrets) on deploy.
# The real secrets.toml is gitignored — never commit your key.

GEMINI_API_KEY = "your_actual_key_here"
